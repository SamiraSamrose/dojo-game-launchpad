# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: dojo-launchpad-service
  labels:
    app: dojo-launchpad
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: dojo-launchpad
